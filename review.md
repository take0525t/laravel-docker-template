# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
select * from `todos`が実行されている。

### Todoモデルのallメソッドの返り値は何か
Collectionクラスのインスタンスが返ってきている。

### 配列の代わりにCollectionクラスを使用するメリットは
Collectionクラスには多様なメソッドがあり、PHPで利用可能な配列操作を簡単にできる。

### view関数の第1・第2引数の指定と何をしているか
第一引数は、画面に表示したいbladeファイルを指定し、第二引数は、渡したいデータの連想配列を指定しています。
第一引数の指定方法は、(フォルダ名.ファイル名)で指定する。

### index.blade.phpの$todoに代入されているものは何か
$todoにはtodosテーブルのレコード情報が配列型で代入されている。

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
フォームから送信された、_token、contentを配列型で取得している。


### fillメソッドは何をしているか
Todoインスタンスの各プロパティに一括で代入している。

### $fillableは何のために設定しているか
fillabelを設定しないとfillメソッド実行時にほかのユーザIDなどで入力内容を登録することができてしまうため。

### saveメソッドで実行しているSQLは何か
INSERT文insert into `todos` (`content`, `updated_at`, `created_at`) values (?, ?, ?)が実行されている。

### redirect()->route()は何をしているか
route()で生成されたURLへリダイレクトさせている。
TodoController.phpの場合はindex.blade.phpへ転送している。

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
PHPの記述でDBを操作することができる。
Gitで共有することで開発者全員が同じテーブルを作成することができる。

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up()はphp artisan migrateを実行した際に呼び出される。
down()はphp artisan migrate:rollbackを実行した際に呼び出される。

### Seederクラスの役割は何か
初期データやテストデータの導入を行う。
テストデータを導入する目的は開発者間のテストデータに差異が生じないようにするため。

### route関数の引数・返り値・使用するメリット
引数はルート名を指定し、返り値は引数にしたルート名のURLが返ってくる。
route関数を使用するメリットは、引数に指定したルート名からURLが作成されるため、ルート名に変更がない限り修正箇所が一点でよいこと。

### @extends・@section・@yieldの関係性とbladeを分割するメリット

@extendsは継承する親Bladeファイルの指定
@sectionは@endsectionまでの囲われた部分を親Bladeファイルに挿入している。
@yieldは@sectionの挿入先となっている。

bladeを分割をするメリットは重複しているコードを共通化して再利用できること。

### @csrfは何のための記述か
csrf攻撃の対策で記述されている。

### {{ }}とは
PHPの処理として認識させる場合に必要な記述。